# Work Order: Run docker dev server

- work_order_id: `2026-04-17-run-docker-dev-server`
- requested_by: human
- date: 2026-04-17
- objective: Start the project using the repository's Docker Compose workflow and report where it can be viewed.

## Requested outcome
- Run the project in Docker using the repo-defined container development path.
- Confirm the service is up.
- Report the URL the human should open.

## Notes
- This is an operational run task, not a code-change task.
- Prefer the exact workflow defined by `docker-compose.yml` and runtime docs.
