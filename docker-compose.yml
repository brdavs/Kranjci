version: "3.9"

services:
  app:
    build: .
    command: npm run dev -- --host 0.0.0.0
    ports:
      - "5173:5173"
    environment:
      NODE_ENV: development
    volumes:
      - .:/app
      - /app/node_modules
